import React from 'react';

const Normal = ({lable}) => {
    return (
        <div className=" rounded-xl bg-green-600 text-white px-4 interface:text-2xl text-center">
            {lable}
        </div>
    );
}

export default Normal;
